import React from 'react';
import Layout from '../../components/Layout';

const AddNewAttribute = (props) => {
  console.log(props);
  return <Layout title="New Attribute"></Layout>;
};

export default AddNewAttribute;
